/**
 * Classe interne permettant de suivre les composants de communication enfants
 */
export declare class Child {
    window: Window;
    readonly handshakeFinishedPromise: Promise<void>;
    handshakeFinishedResolver: () => void;
    childUUID: string;
    childParentVisionUUID: string;
    constructor(window: Window, timeoutDelaiMS: number, timeoutCallback: any);
}
